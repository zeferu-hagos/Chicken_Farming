// routes/incubationManagementRoutes.js
const express = require('express');
const router = express.Router();
const IncubationManagement = require('../models/IncubationManagement');

// Define routes here

module.exports = router;
